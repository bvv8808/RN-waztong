import React from 'react';
import {View} from 'react-native';

interface Props {
  navigation: any;
}

const CoinStoreScreen = ({navigation}: Props) => {
  return <View></View>;
};

export default CoinStoreScreen;
